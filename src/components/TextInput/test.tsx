import { screen } from '@testing-library/react';
import { TextInput } from '.';
import { renderTheme } from '../../styles/render-theme';

// describe('<TextInput />', () => {
//   it('should render', () => {
//     renderTheme(<TextInput />);
//     expect(screen.getByRole('heading')).toBeInTheDocument();
//   });
// });
